---
phase: 05-project-init
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/init/SKILL.md
  - skills/status/SKILL.md
  - internal/cli/init.go
  - internal/cli/init_test.go
  - internal/cli/status.go
  - internal/cli/status_test.go
  - internal/cli/root.go
autonomous: true
requirements: [INIT-01, INIT-02, CMD-01]
must_haves:
  truths:
    - "skills/init/SKILL.md exists at plugin root, making /gsd-wired:init available as a slash command"
    - "SKILL.md contains the ~12 question flow with correct categories (what, why, who, constraints, done criteria, tech stack, prior art, risks)"
    - "SKILL.md instructs Claude to ask one question at a time and wait for response (D-04)"
    - "SKILL.md supports three modes: full (12 questions), quick (3 questions), PR/issue (D-05)"
    - "SKILL.md instructs Claude to call init_project MCP tool after collecting answers"
    - "skills/status/SKILL.md exists and instructs Claude to call get_status MCP tool"
    - "gsdw init CLI subcommand is registered and runs bd init + writes template files"
    - "gsdw status CLI subcommand is registered and queries graph for display"
  artifacts:
    - path: "skills/init/SKILL.md"
      provides: "/gsd-wired:init slash command with questioning flow"
      contains: "disable-model-invocation: true"
    - path: "skills/status/SKILL.md"
      provides: "/gsd-wired:status slash command"
      contains: "get_status"
    - path: "internal/cli/init.go"
      provides: "gsdw init CLI subcommand"
      contains: "func NewInitCmd"
    - path: "internal/cli/status.go"
      provides: "gsdw status CLI subcommand"
      contains: "func NewStatusCmd"
    - path: "internal/cli/root.go"
      provides: "Updated root with NewInitCmd and NewStatusCmd"
      contains: "NewInitCmd"
  key_links:
    - from: "skills/init/SKILL.md"
      to: "init_project MCP tool"
      via: "Claude calls init_project after questioning"
      pattern: "init_project"
    - from: "skills/status/SKILL.md"
      to: "get_status MCP tool"
      via: "Claude calls get_status for dashboard data"
      pattern: "get_status"
    - from: "internal/cli/root.go"
      to: "internal/cli/init.go"
      via: "root.AddCommand(NewInitCmd())"
      pattern: "NewInitCmd"
---

<objective>
Create the slash command SKILL.md files and CLI subcommands for /gsd-wired:init and /gsd-wired:status.

Purpose: This is the user-facing layer (D-01). The SKILL.md files define how Claude conducts the interactive questioning flow and renders the status dashboard. The CLI subcommands provide standalone gsdw init/status for direct terminal use. Per D-02, the developer never thinks about bead structure -- SKILL.md translates their answers into MCP tool calls.

Output: Two skills (auto-discovered slash commands), two CLI subcommands registered in root, with tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-project-init/05-CONTEXT.md
@.planning/phases/05-project-init/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From .claude-plugin/plugin.json:
Plugin name is "gsd-wired", so skills at skills/init/SKILL.md become /gsd-wired:init.
Plugin has: name, version, description, author fields only. No commands field needed.

From internal/cli/root.go:
```go
func NewRootCmd() *cobra.Command {
    // ...
    root.AddCommand(NewVersionCmd(), NewServeCmd(), NewHookCmd(), NewBdCmd(), NewReadyCmd())
    return root
}
```

From internal/cli/ready.go (pattern to follow):
```go
func NewReadyCmd() *cobra.Command {
    cmd := &cobra.Command{
        Use:   "ready",
        Short: "Show unblocked tasks ready to work on",
        RunE: func(cmd *cobra.Command, args []string) error {
            beadsDir, err := findBeadsDir()
            client, err := graph.NewClient(beadsDir)
            return renderReadyTree(cmd.OutOrStdout(), ...)
        },
    }
    return cmd
}
func findBeadsDir() (string, error) // reuse this
func phaseNumFromBead(b graph.Bead) int // reuse this
```

From internal/mcp/init.go — bd init args pattern:
bd init --quiet --skip-hooks --skip-agents with BEADS_DIR=cwd

SKILL.md frontmatter fields (from 05-RESEARCH.md):
name, description, disable-model-invocation (true), argument-hint
Skills are auto-discovered from skills/ directory at plugin root.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md files for init and status slash commands</name>
  <files>skills/init/SKILL.md, skills/status/SKILL.md</files>

  <read_first>
    - .planning/phases/05-project-init/05-RESEARCH.md (SKILL.md patterns, frontmatter, anti-patterns)
    - .planning/phases/05-project-init/05-CONTEXT.md (D-01 through D-12 decisions)
    - .claude-plugin/plugin.json (plugin name for namespace)
  </read_first>

  <action>
    Create skills/init/SKILL.md at the PLUGIN ROOT (NOT inside .claude-plugin/ per Pitfall 1):

    YAML frontmatter with fields: name=init, description="Initialize a new gsd-wired project with guided questioning. Use when starting a new project.", disable-model-invocation=true, argument-hint="[full|quick|pr]"

    Body content (natural language instructions for Claude):

    1. Opening section: "Initialize a gsd-wired project using $ARGUMENTS mode (default: full)."

    2. Role section (per D-01, D-02): "You are a builder-partner helping set up a new project. Ask questions one at a time, wait for each answer before asking the next. Use GSD-familiar language (phases, plans, waves). Never mention beads, bead IDs, or graph structures to the developer."

    3. Full init section (per D-03): 12 questions in this order, one at a time:
       1. "What are you building?" (the what -- be specific about the product/tool/feature)
       2. "Why are you building it?" (the motivation -- what problem does this solve?)
       3. "Who is it for?" (target users/audience)
       4. "What does success look like?" (measurable done criteria)
       5. "What's the tech stack?" (languages, frameworks, infrastructure)
       6. "What constraints exist?" (time, budget, team size, platform requirements)
       7. "What's the prior art?" (existing solutions, competitors, inspiration)
       8. "What are the risks?" (technical, market, dependency risks)
       9. "What does v1 look like?" (minimum viable scope)
       10. "What's explicitly out of scope?" (boundaries)
       11. "Any non-negotiable requirements?" (hard constraints)
       12. "Anything else I should know?" (catch-all)

    4. Quick init section (per D-05): 3 essential questions only:
       1. "What are you building?"
       2. "Why are you building it?"
       3. "What does done look like?"

    5. PR/Issue mode section (per D-05): Accept a PR URL or issue URL, ask 3-4 questions about the work context:
       1. "Share the PR/issue URL"
       2. "What's your role in this work?" (reviewer, contributor, owner)
       3. "What context do you need tracked?" (decisions, blockers, progress)
       4. "Any dependencies or constraints?"

    6. After questioning section: "Once you have all answers, call the `init_project` MCP tool with the collected context as a single JSON object. Map each answer to the corresponding field:
       - project_name: Derive a concise project name from the 'what' answer
       - what, why, who, done_criteria, tech_stack, constraints, risks: Direct from answers
       - mode: The init mode used (full, quick, or pr)
       - pr_url: The PR/issue URL if in pr mode"

    7. Post-init section (per D-08): "After the tool returns successfully, display:
       - 'Project initialized: {project_name}'
       - Brief summary of what was created
       - 'Ready to proceed? (auto-continuing in 30 seconds...)'
       Wait for the developer's response. If they say nothing, auto-proceed to suggest next steps (running /gsd-wired:status, planning first phase)."

    Create skills/status/SKILL.md:

    YAML frontmatter with fields: name=status, description="Show current project status from the beads graph", disable-model-invocation=true

    Body content:
    1. "Show the current project status by calling the `get_status` MCP tool."
    2. "Render the result as a dashboard using GSD-familiar terms (per D-09, D-10):
       - Project name as header
       - Current phase with progress indicator
       - Ready tasks (next wave) as a bullet list
       - Use phases/plans/waves terminology, never bead/graph terminology"
    3. "If no project is initialized, tell the developer to run /gsd-wired:init first."

    IMPORTANT: skills/ directory is at the plugin root: /Users/trekkie/projects/gsd-wired/skills/init/SKILL.md. NOT inside .claude-plugin/.
  </action>

  <verify>
    <automated>cd /Users/trekkie/projects/gsd-wired && test -f skills/init/SKILL.md && test -f skills/status/SKILL.md && grep -q "disable-model-invocation: true" skills/init/SKILL.md && grep -q "init_project" skills/init/SKILL.md && grep -q "get_status" skills/status/SKILL.md && echo "PASS"</automated>
  </verify>

  <acceptance_criteria>
    - test -f skills/init/SKILL.md (file exists at plugin root)
    - test -f skills/status/SKILL.md (file exists at plugin root)
    - grep -q "disable-model-invocation: true" skills/init/SKILL.md
    - grep -q "argument-hint" skills/init/SKILL.md
    - grep -q "init_project" skills/init/SKILL.md (references MCP tool)
    - grep -q "get_status" skills/status/SKILL.md (references MCP tool)
    - grep -q "What are you building" skills/init/SKILL.md (first question present)
    - grep -q "full|quick|pr" skills/init/SKILL.md or equivalent mode sections exist
  </acceptance_criteria>

  <done>SKILL.md files exist at plugin root with correct frontmatter, questioning flow matches D-03/D-04/D-05, and both reference their corresponding MCP tools.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gsdw init and gsdw status CLI subcommands</name>
  <files>internal/cli/init.go, internal/cli/init_test.go, internal/cli/status.go, internal/cli/status_test.go, internal/cli/root.go</files>

  <read_first>
    - internal/cli/root.go (NewRootCmd, AddCommand pattern)
    - internal/cli/ready.go (NewReadyCmd pattern, findBeadsDir, cmd.OutOrStdout)
    - internal/graph/client.go (NewClient constructor)
    - internal/graph/query.go (QueryByLabel, ListReady)
    - internal/hook/session_start.go (buildSessionContext, formatSessionContext patterns)
  </read_first>

  <behavior>
    - Test: TestRootCmdHasInit -- NewRootCmd() has subcommand "init" registered
    - Test: TestRootCmdHasStatus -- NewRootCmd() has subcommand "status" registered
    - Test: TestInitCmdWritesFiles -- Run init cmd in a temp dir, verify PROJECT.md template and .gsdw/config.json are created
    - Test: TestStatusCmdOutput -- Run status cmd with a fake bd that returns phase/ready data, verify output contains "Phase" and task info
  </behavior>

  <action>
    Create internal/cli/init.go:

    1. func NewInitCmd() *cobra.Command:
       - Use: "init"
       - Short: "Initialize beads directory and project files"
       - Long: Explains this is for direct CLI use; the /gsd-wired:init slash command uses the MCP tool instead.
       - RunE handler:
         a. cwd from os.Getwd()
         b. Check if .beads/ exists at cwd; if not, run bd init:
            - Look up bd via exec.LookPath (follow findBeadsDir pattern from ready.go)
            - Run: bd init --quiet --skip-hooks --skip-agents with BEADS_DIR=cwd (same args as serverState.runBdInit)
            - Print "Initialized .beads/ directory" to cmd.OutOrStdout()
         c. Write PROJECT.md template to cwd if it doesn't exist:
            - Minimal template with placeholders: "# Project Name\n\n## What\n(describe)\n\n## Why\n(motivation)\n..."
            - Print "Created PROJECT.md" to cmd.OutOrStdout()
         d. Create .gsdw/ dir and write .gsdw/config.json if doesn't exist:
            - {"project_name": "", "initialized": "{timestamp}", "mode": "cli"}
            - Print "Created .gsdw/config.json" to cmd.OutOrStdout()
       - IMPORTANT: Use cmd.OutOrStdout() for ALL output (never fmt.Println). Per Pitfall 5.

    Create internal/cli/status.go:

    1. func NewStatusCmd() *cobra.Command:
       - Use: "status"
       - Short: "Show project status from beads graph"
       - RunE handler:
         a. beadsDir from findBeadsDir() (reuse from ready.go, already exported in that file)
         b. client from graph.NewClient(beadsDir)
         c. Query phases: client.QueryByLabel(ctx, "gsd:phase")
         d. Find current phase (highest open phase, same logic as buildSessionContext)
         e. Query ready: client.ListReady(ctx)
         f. Render status to cmd.OutOrStdout() using a renderStatus function:
            - Header: "GSD Project Status"
            - Current phase line: "Current Phase: {title} (Phase {N})"
            - Ready tasks as bullet list: "- Plan {planID}: {title}"
            - If no phases: "No project initialized. Run gsdw init first."

    Update internal/cli/root.go:
    - Change AddCommand line to include NewInitCmd() and NewStatusCmd():
      root.AddCommand(NewVersionCmd(), NewServeCmd(), NewHookCmd(), NewBdCmd(), NewReadyCmd(), NewInitCmd(), NewStatusCmd())

    Create internal/cli/init_test.go:
    - TestRootCmdHasInit: iterate root.Commands(), verify "init" subcommand exists
    - TestInitCmdWritesFiles: create temp dir, set it as working dir, run init cmd, verify PROJECT.md and .gsdw/config.json exist

    Create internal/cli/status_test.go:
    - TestRootCmdHasStatus: iterate root.Commands(), verify "status" subcommand exists
    - TestStatusCmdOutput: test renderStatus with constructed phase/ready data (pure function test, no bd needed -- same pattern as renderReadyTree tests in ready_test.go)

    IMPORTANT: findBeadsDir is already defined in ready.go and exported at package level. Do NOT redefine it.
    IMPORTANT: phaseNumFromBead is already defined in ready.go. Reuse it in status.go.
  </action>

  <verify>
    <automated>cd /Users/trekkie/projects/gsd-wired && go test ./internal/cli/... -run "TestRootCmdHas|TestInitCmd|TestStatusCmd" -v -race</automated>
  </verify>

  <acceptance_criteria>
    - grep -q "func NewInitCmd" internal/cli/init.go
    - grep -q "func NewStatusCmd" internal/cli/status.go
    - grep -q "NewInitCmd" internal/cli/root.go
    - grep -q "NewStatusCmd" internal/cli/root.go
    - grep -q "TestRootCmdHasInit" internal/cli/init_test.go
    - grep -q "TestRootCmdHasStatus" internal/cli/status_test.go
    - go test ./internal/cli/... -run TestRootCmdHasInit -v passes
    - go test ./internal/cli/... -run TestRootCmdHasStatus -v passes
    - go test ./... -race all pass (full regression)
  </acceptance_criteria>

  <done>gsdw init creates .beads/ + template files. gsdw status shows project state from graph. Both registered in root command. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
# CLI package tests
cd /Users/trekkie/projects/gsd-wired && go test ./internal/cli/... -v -race

# Verify skill files exist at plugin root
ls -la skills/init/SKILL.md skills/status/SKILL.md

# Verify root command registers init and status
cd /Users/trekkie/projects/gsd-wired && go test ./internal/cli/... -run "TestRootCmdHas" -v

# Full project regression
cd /Users/trekkie/projects/gsd-wired && go test ./... -race
```
</verification>

<success_criteria>
- /gsd-wired:init slash command exists via skills/init/SKILL.md with 12-question flow (D-03)
- /gsd-wired:status slash command exists via skills/status/SKILL.md calling get_status
- gsdw init CLI creates .beads/ directory and template PROJECT.md + config.json
- gsdw status CLI shows project state from beads graph queries
- Interactive questioning is one-at-a-time per D-04
- Three modes supported per D-05 (full, quick, PR/issue)
- All tests pass with -race across the full project
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-init/05-02-SUMMARY.md`
</output>
