---
phase: 09-token-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/graph/bead.go
  - internal/graph/tier.go
  - internal/graph/tier_test.go
  - internal/graph/update.go
  - internal/graph/query.go
autonomous: true
requirements: [TOKEN-01, TOKEN-03, TOKEN-04, TOKEN-05]

must_haves:
  truths:
    - "classifyTier returns hot for open/in_progress beads, warm for recently closed, cold for old closed"
    - "estimateTokens returns len(s)/4 with min 1 for non-empty strings"
    - "ClosePlan writes gsd:compact metadata after closing a bead (best-effort)"
    - "QueryTiered returns beads classified into hot/warm/cold tiers"
    - "TieredBead carries tier assignment and pre-rendered compact string"
  artifacts:
    - path: "internal/graph/tier.go"
      provides: "Tier constants, TieredBead type, classifyTier, estimateTokens, compactSummary, formatHot/Warm/Cold"
      min_lines: 80
    - path: "internal/graph/tier_test.go"
      provides: "Tests for classifyTier, estimateTokens, TieredBead, CompactBead, QueryTiered"
      min_lines: 100
  key_links:
    - from: "internal/graph/update.go"
      to: "internal/graph/tier.go"
      via: "ClosePlan calls compactSummary + UpdateBeadMetadata for gsd:compact"
      pattern: "compactSummary|gsd:compact"
    - from: "internal/graph/query.go"
      to: "internal/graph/tier.go"
      via: "QueryTiered calls classifyTier to classify beads"
      pattern: "classifyTier"
---

<objective>
Graph-layer tiering: Tier constants, TieredBead type, classification, token estimation, compaction on close, and tiered query.

Purpose: Foundation for all token-aware context routing. Every downstream consumer (SessionStart, execute_wave, get_tiered_context) depends on these graph-layer primitives.
Output: internal/graph/tier.go with types and pure functions, updated ClosePlan with compaction, QueryTiered method.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-token-context/09-CONTEXT.md
@.planning/phases/09-token-context/09-RESEARCH.md

<interfaces>
<!-- Existing types and methods the executor needs -->

From internal/graph/bead.go:
```go
type Bead struct {
    ID                 string         `json:"id"`
    Title              string         `json:"title"`
    Description        string         `json:"description,omitempty"`
    AcceptanceCriteria string         `json:"acceptance_criteria,omitempty"`
    Status             string         `json:"status"`
    ClosedAt           *time.Time     `json:"closed_at,omitempty"`
    CloseReason        string         `json:"close_reason,omitempty"`
    Metadata           map[string]any `json:"metadata,omitempty"`
    Labels             []string       `json:"labels,omitempty"`
    // ... other fields
}
```

From internal/graph/update.go:
```go
func (c *Client) ClosePlan(ctx context.Context, beadID, reason string) (*Bead, []Bead, error)
func (c *Client) UpdateBeadMetadata(ctx context.Context, beadID string, meta map[string]any) (*Bead, error)
func (c *Client) AddLabel(ctx context.Context, beadID, label string) (*Bead, error)
```

From internal/graph/query.go:
```go
func (c *Client) QueryByLabel(ctx context.Context, label string) ([]Bead, error)
func (c *Client) ListReady(ctx context.Context) ([]Bead, error)
func (c *Client) GetBead(ctx context.Context, beadID string) (*Bead, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tier.go with types and pure functions + tier_test.go</name>
  <files>internal/graph/tier.go, internal/graph/tier_test.go</files>
  <read_first>
    - internal/graph/bead.go (Bead struct, ClosedAt *time.Time)
    - internal/graph/query.go (QueryByLabel signature — model for QueryTiered)
    - internal/graph/update.go (UpdateBeadMetadata — used by CompactBead)
    - .planning/phases/09-token-context/09-RESEARCH.md §Pattern 1-3 (exact type definitions)
  </read_first>
  <behavior>
    - TestClassifyTier: open bead returns TierHot; in_progress returns TierHot; closed bead with ClosedAt after threshold returns TierWarm; closed bead with ClosedAt before threshold returns TierCold; closed bead with nil ClosedAt returns TierCold
    - TestEstimateTokens: empty string returns 0; "abcd" (4 bytes) returns 1; "ab" (2 bytes) returns 1 (min 1 for non-empty); 400-byte string returns 100
    - TestTieredBead: TieredBead embeds Bead and carries Tier + Compact fields; JSON marshal includes tier field
    - TestFormatHot: returns "## [HOT] {title} ({id})\n" with Goal and Done-when lines when Description/AcceptanceCriteria present
    - TestFormatWarm: returns "- [WARM] {title}: {close_reason}\n"
    - TestFormatCold: returns "- [{id}] {title}\n"
    - TestCompactSummary: bead with CloseReason returns "title: reason"; bead without CloseReason returns "title"
    - TestQueryTiered: classifies a mix of open/closed beads into hot/warm/cold arrays correctly; warm count limited to warmCount parameter
  </behavior>
  <action>
    Create internal/graph/tier.go with:

    1. Tier type (string) with constants TierHot, TierWarm, TierCold per D-03/D-04/D-05.

    2. TieredBead struct embedding Bead with Tier and Compact fields per Research Pattern 1. JSON tags: `json:"tier"` and `json:"compact,omitempty"`.

    3. classifyTier(b Bead, warmIDs map[string]bool) Tier — pure function. Per D-03: open/in_progress = hot. If bead.ID is in warmIDs = warm. Otherwise cold. This uses count-based warm (Research Open Question 1 recommendation) instead of time-based.

    4. estimateTokens(s string) int — per D-07: len(s)/4 with min 1 for non-empty. Single source of truth.

    5. compactSummary(b *Bead) string — returns "title: close_reason" or just "title" if no CloseReason.

    6. formatHot(b TieredBead) string — per Research Pattern (tiered format functions). Uses fmt.Fprintf into strings.Builder. Includes Title, ID, Description (as Goal), AcceptanceCriteria (as Done when).

    7. formatWarm(b TieredBead) string — "- [WARM] {title}: {close_reason}\n"

    8. formatCold(b TieredBead) string — "- [{id}] {title}\n"

    9. CompactBead method on Client — writes compact summary to Metadata["gsd:compact"] via UpdateBeadMetadata. Best-effort, errors returned but callers log and continue. Per Research Pattern 5.

    10. QueryTiered method on Client — queries beads by label, sorts closed beads by ClosedAt desc, takes first warmCount as warm set, classifies all via classifyTier, returns (hot, warm, cold []TieredBead, error). Renders Compact field for warm/cold from compactSummary or Metadata["gsd:compact"] if present. Per Research Pattern 2 and Open Question 1 (count-based warm, default 5).

    Write tests FIRST in tier_test.go. classifyTier and estimateTokens are pure functions — test without fake_bd. CompactBead and QueryTiered need fake_bd — follow existing patterns in internal/graph/ tests.
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-wired && go test ./internal/graph/... -run "TestClassifyTier|TestEstimateTokens|TestTieredBead|TestFormat|TestCompactSummary|TestCompactBead|TestQueryTiered" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "TierHot.*Tier.*=.*hot" internal/graph/tier.go
    - grep -q "TierWarm.*Tier.*=.*warm" internal/graph/tier.go
    - grep -q "TierCold.*Tier.*=.*cold" internal/graph/tier.go
    - grep -q "type TieredBead struct" internal/graph/tier.go
    - grep -q "func classifyTier" internal/graph/tier.go
    - grep -q "func estimateTokens" internal/graph/tier.go
    - grep -q "func compactSummary" internal/graph/tier.go
    - grep -q "func (c \*Client) CompactBead" internal/graph/tier.go
    - grep -q "func (c \*Client) QueryTiered" internal/graph/tier.go
    - grep -q "func formatHot" internal/graph/tier.go
    - grep -q "func formatWarm" internal/graph/tier.go
    - grep -q "func formatCold" internal/graph/tier.go
    - grep -q "TestClassifyTier" internal/graph/tier_test.go
    - grep -q "TestEstimateTokens" internal/graph/tier_test.go
    - grep -q "TestQueryTiered" internal/graph/tier_test.go
  </acceptance_criteria>
  <done>All tier types defined, pure functions pass, CompactBead writes gsd:compact metadata, QueryTiered classifies beads into hot/warm/cold with count-based warm threshold</done>
</task>

<task type="auto">
  <name>Task 2: Add compaction to ClosePlan</name>
  <files>internal/graph/update.go</files>
  <read_first>
    - internal/graph/update.go (current ClosePlan implementation)
    - internal/graph/tier.go (compactSummary, CompactBead — created in Task 1)
    - .planning/phases/09-token-context/09-RESEARCH.md §Pattern 5 (compaction on close)
  </read_first>
  <action>
    Modify ClosePlan in update.go to call compaction after the close succeeds, per D-12 and Research Pattern 5.

    After the close succeeds and before the unblocked detection:
    1. Call compactSummary(&closed[0]) to get the summary text.
    2. Call c.CompactBead(ctx, beadID, summary) — best-effort: if it fails, slog.Warn and continue. Per Pitfall 3: only compact closed beads (the guard is implicit since we just closed it).
    3. Add import for "log/slog" if not already present.

    This is the write-time compaction trigger per Research "critical insight": compaction is a write-time operation triggered when a bead closes.

    Do NOT compact open beads (Pitfall 3). The guard is structural: CompactBead is only called inside ClosePlan after close succeeds.
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-wired && go test ./internal/graph/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "compactSummary" internal/graph/update.go
    - grep -q "CompactBead" internal/graph/update.go
    - grep -q "slog.Warn" internal/graph/update.go
    - grep -q "gsd:compact" internal/graph/update.go || grep -q "compaction" internal/graph/update.go
  </acceptance_criteria>
  <done>ClosePlan writes gsd:compact metadata after close (best-effort). Existing ClosePlan tests still pass. No behavioral change to close — compaction is additive.</done>
</task>

</tasks>

<verification>
All graph package tests pass: `go test -race ./internal/graph/... -v -count=1`
Full suite still green: `go test -race ./...`
</verification>

<success_criteria>
- Tier, TieredBead, classifyTier, estimateTokens, compactSummary, formatHot/Warm/Cold, CompactBead, QueryTiered all exist and are tested
- ClosePlan calls CompactBead after close (best-effort)
- No regressions in existing 164+ tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-token-context/09-01-SUMMARY.md`
</output>
