---
phase: 06-research-planning
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/mcp/create_plan_beads.go
  - internal/mcp/create_plan_beads_test.go
  - internal/mcp/tools.go
  - internal/mcp/server.go
  - internal/mcp/server_test.go
  - internal/mcp/tools_test.go
  - internal/cli/plan.go
  - internal/cli/plan_test.go
  - internal/cli/root.go
  - skills/plan/SKILL.md
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, CMD-03]

must_haves:
  truths:
    - "create_plan_beads MCP tool batch-creates task beads with dependency wiring from a JSON plan"
    - "Each task bead has acceptance criteria, complexity, and file touch list in metadata"
    - "Local task IDs (e.g. 06-01) are resolved to actual bead IDs for dependency wiring"
    - "/gsd-wired:plan slash command auto-generates a full plan from research + context"
    - "Plan checker validates requirement coverage before approving the plan"
    - "gsdw plan CLI subcommand exists and is registered"
  artifacts:
    - path: "internal/mcp/create_plan_beads.go"
      provides: "handleCreatePlanBeads handler with topological dependency resolution"
      exports: ["handleCreatePlanBeads"]
    - path: "internal/mcp/create_plan_beads_test.go"
      provides: "Unit tests for create_plan_beads tool"
      contains: "TestCreatePlanBeads"
    - path: "skills/plan/SKILL.md"
      provides: "/gsd-wired:plan slash command with plan checker loop"
      contains: "create_plan_beads"
    - path: "internal/cli/plan.go"
      provides: "gsdw plan CLI subcommand"
      contains: "NewPlanCmd"
  key_links:
    - from: "skills/plan/SKILL.md"
      to: "internal/mcp/create_plan_beads.go"
      via: "MCP tool call create_plan_beads"
      pattern: "create_plan_beads"
    - from: "internal/mcp/create_plan_beads.go"
      to: "internal/graph/create.go"
      via: "CreatePlan calls with dependency bead IDs"
      pattern: "state\\.client\\.CreatePlan"
    - from: "skills/plan/SKILL.md"
      to: "query_by_label"
      via: "requirement coverage check in validation loop"
      pattern: "query_by_label"
---

<objective>
Planning workflow MCP tool, CLI subcommand, and SKILL.md slash command with plan checker.

Purpose: Enable plan decomposition of phase epics into dependency-aware task beads. `create_plan_beads` batch-creates task beads from a structured JSON plan, resolving local IDs to bead IDs in topological order. SKILL.md orchestrates plan generation from research output, displays GSD-familiar wave/task format per D-08, and validates requirement coverage inline per D-10/D-12.

Output: 1 new MCP tool (create_plan_beads), 1 CLI subcommand (gsdw plan), 1 SKILL.md (/gsd-wired:plan). Tool count 12 -> 13. All existing tests remain green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-research-planning/06-RESEARCH.md
@.planning/phases/06-research-planning/06-01-SUMMARY.md

@internal/mcp/tools.go
@internal/mcp/run_research.go
@internal/mcp/init_project.go
@internal/mcp/tools_test.go
@internal/mcp/server.go
@internal/mcp/server_test.go
@internal/graph/create.go
@internal/graph/query.go
@internal/graph/update.go
@internal/graph/bead.go
@internal/cli/root.go
@internal/cli/research.go
@skills/init/SKILL.md
@skills/research/SKILL.md

<interfaces>
<!-- Key types and contracts from Plan 01 output + existing codebase. -->

From internal/mcp/run_research.go (created in Plan 01):
```go
type runResearchResult struct {
    EpicBeadID   string            `json:"epic_bead_id"`
    ChildBeadIDs map[string]string `json:"child_bead_ids"`
}
// handleRunResearch creates research epic + 4 child beads
// handleSynthesizeResearch creates summary bead
```

From internal/graph/create.go:
```go
func (c *Client) CreatePlan(ctx context.Context, planID string, phaseNum int, parentBeadID, title, acceptance, planContext string, reqIDs []string, depBeadIDs []string) (*Bead, error)
```

From internal/graph/query.go:
```go
func (c *Client) QueryByLabel(ctx context.Context, label string) ([]Bead, error)
func (c *Client) ListReady(ctx context.Context) ([]Bead, error)
```

From internal/mcp/tools.go:
```go
func toolError(msg string) *mcpsdk.CallToolResult
func toolResult(data any) (*mcpsdk.CallToolResult, error)
// registerTools adds all tools; after Plan 01 there are 12 tools
```

Tool registration pattern (from tools.go):
```go
server.AddTool(&mcpsdk.Tool{
    Name: "tool_name",
    Description: "...",
    InputSchema: json.RawMessage(`{...}`),
}, func(ctx context.Context, req *mcpsdk.CallToolRequest) (*mcpsdk.CallToolResult, error) {
    var args toolArgs
    if err := json.Unmarshal(req.Params.Arguments, &args); err != nil {
        return toolError("invalid arguments: " + err.Error()), nil
    }
    return handleToolName(ctx, state, args)
})
```

CLI subcommand pattern (from root.go):
```go
root.AddCommand(NewVersionCmd(), NewServeCmd(), NewHookCmd(), NewBdCmd(), NewReadyCmd(), NewInitCmd(), NewStatusCmd(), NewResearchCmd())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: create_plan_beads MCP tool with TDD</name>
  <files>internal/mcp/create_plan_beads.go, internal/mcp/create_plan_beads_test.go, internal/mcp/tools.go, internal/mcp/server.go, internal/mcp/server_test.go, internal/mcp/tools_test.go</files>
  <read_first>internal/mcp/run_research.go, internal/mcp/tools.go, internal/mcp/tools_test.go, internal/mcp/server.go, internal/mcp/server_test.go, internal/graph/create.go</read_first>
  <behavior>
    - TestCreatePlanBeads: calling create_plan_beads with phase_num=6, epic_bead_id="epic-123", and tasks array containing 2 tasks (one depends on the other) returns JSON with task_bead_ids map having 2 entries, each with non-empty bead ID
    - TestCreatePlanBeadsMetadata: each created task bead has complexity and files fields in metadata (verifiable via fake_bd capture file args containing "--metadata" with "complexity" and "files")
    - TestCreatePlanBeadsReqIDs: task beads get their req_ids as labels (verifiable via fake_bd capture)
    - TestCreatePlanBeadsDeps: second task's CreatePlan call includes the first task's bead ID in --deps (dependency resolution from local IDs)
    - TestToolsRegistered count increases from 12 to 13
    - TestToolsRegistered wantNames includes "create_plan_beads"
  </behavior>
  <action>
    **RED phase:** Create internal/mcp/create_plan_beads_test.go with tests following the established connectInProcess pattern. Tests:
    1. TestCreatePlanBeads: call with 2 tasks where task "06-02" depends_on ["06-01"]. Verify response has task_bead_ids map with both keys.
    2. TestCreatePlanBeadsNoDeps: call with 1 task with no dependencies. Verify success.
    3. TestCreatePlanBeadsBadEpic: call with empty epic_bead_id. Expect IsError=true.
    Update tools_test.go TestToolsRegistered: change len check from 12 to 13, add "create_plan_beads" to wantNames.
    Update server_test.go TestToolsListed: change len check from 12 to 13, add "create_plan_beads" to wantNames.
    Run tests -- MUST fail.

    **GREEN phase:** Create internal/mcp/create_plan_beads.go with:
    - planTask struct: ID string, Title string, Acceptance string, Context string, ReqIDs []string, DependsOn []string, Complexity string, Files []string (matches research JSON schema from Pattern 3)
    - createPlanBeadsArgs struct: PhaseNum int, EpicBeadID string, Tasks []planTask
    - createPlanBeadsResult struct: TaskBeadIDs map[string]string `json:"task_bead_ids"`
    - handleCreatePlanBeads(ctx, state, args):
      1. state.init(ctx)
      2. Validate EpicBeadID non-empty
      3. Topological sort: build adjacency from task.DependsOn, process tasks with no deps first, then those whose deps are all resolved. Use a simple iterative approach (not recursive) per Pitfall 3.
      4. For each task in topological order:
         - Resolve depBeadIDs from localToBead map
         - Build metadata: {"complexity": task.Complexity, "files": task.Files, "gsd_plan": task.ID}
         - Call state.client.CreatePlan with resolved deps
         - Store localToBead[task.ID] = bead.ID
      5. Return createPlanBeadsResult with all mappings

    Update internal/mcp/tools.go registerTools():
    - Add create_plan_beads tool registration with InputSchema:
      - phase_num (integer, required)
      - epic_bead_id (string, required)
      - tasks (array of objects with id, title, acceptance, context, req_ids, depends_on, complexity, files -- all required except depends_on/req_ids)
    - Update comment from "12 GSD MCP tools" to "13 GSD MCP tools"

    Update internal/mcp/server.go: change slog.Debug tools count from 12 to 13.

    Run `go test ./internal/mcp/... -race` -- all tests MUST pass.
    Run `go test ./... -race` -- full suite MUST pass.
  </action>
  <verify>
    <automated>go test ./internal/mcp/... -run "TestCreatePlanBeads|TestToolsRegistered" -race -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "func handleCreatePlanBeads" internal/mcp/create_plan_beads.go
    - grep -q "planTask" internal/mcp/create_plan_beads.go
    - grep -q "localToBead" internal/mcp/create_plan_beads.go
    - grep -q "create_plan_beads" internal/mcp/tools.go
    - grep -q '"tools", 13' internal/mcp/server.go
    - grep -q "TestCreatePlanBeads" internal/mcp/create_plan_beads_test.go
    - grep -q "13" internal/mcp/server_test.go (tool count)
    - go test ./internal/mcp/... -race exits 0
    - go test ./... -race exits 0
  </acceptance_criteria>
  <done>create_plan_beads creates task beads in topological order with dependency resolution. Each task bead has complexity, files, and req_ids. Tool count is 13. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: plan CLI subcommand and SKILL.md slash command with plan checker</name>
  <files>internal/cli/plan.go, internal/cli/plan_test.go, internal/cli/root.go, skills/plan/SKILL.md</files>
  <read_first>internal/cli/research.go, internal/cli/research_test.go, internal/cli/root.go, skills/init/SKILL.md, skills/research/SKILL.md</read_first>
  <action>
    **Create skills/plan/SKILL.md** following the research/SKILL.md and init/SKILL.md patterns. Structure:
    - Frontmatter: name: plan, description: "Create a dependency-aware phase plan from research results", disable-model-invocation: true, argument-hint: "[phase_number]"
    - Role section: "You are a planning coordinator. You decompose phase goals into task beads with dependency ordering. Display plans in GSD-familiar format (waves, tasks, dependencies). Never mention beads, bead IDs, or graph structures to the developer." per D-01/D-08.
    - Workflow:
      1. "Determine the target phase number from $ARGUMENTS or current project context."
      2. "Call query_by_label with 'gsd:phase' to find the phase epic bead. Read its goal and acceptance criteria."
      3. "Call query_by_label with 'gsd:research' to find research results. Read the synthesizer summary and individual topic results."
      4. "Auto-generate a full plan from research + phase context per D-07. Decompose the phase goal into 2-5 tasks with:"
         - "Clear title and objective"
         - "Acceptance criteria (how to verify the task is done)"
         - "Estimated complexity: S (< 15min), M (15-30min), L (30-60min)"
         - "File touch list (which files will be created or modified)"
         - "Dependencies (which tasks must complete before this one)"
         - "Requirement IDs covered (from the phase's requirement list)"
      5. "Call create_plan_beads with the generated plan as a JSON array of task objects."
      6. "Call flush_writes to commit all beads to Dolt." per Pitfall 5.

    - Plan display section per D-08:
      "After creating plan beads, display the plan in GSD-familiar format:"
      ```
      ## Phase {N}: {Title}

      ### Wave 1 (parallel)
      - Task {id}: {title} [{complexity}]
        Files: {file_list}
        Reqs: {req_ids}

      ### Wave 2 (after Wave 1)
      - Task {id}: {title} [{complexity}] (depends on: {dep_ids})
        Files: {file_list}
        Reqs: {req_ids}
      ```
      "Use list_ready to determine Wave 1 tasks. Tasks not in ready are in later waves."

    - Plan validation section per D-10/D-11/D-12:
      "## Plan validation (inline, up to 3 iterations)"
      "After creating plan beads, validate the plan:"
      "1. For each requirement ID in the phase, call query_by_label with that ID. If the result is empty, that requirement is uncovered -- a coverage gap."
      "2. Call list_ready to verify Wave 1 tasks have no unresolved dependencies."
      "3. If gaps found: identify missing tasks, call create_plan_beads again with additional tasks to fill gaps."
      "4. Repeat validation up to 3 times total. Track your iteration count explicitly: 'Validation iteration 1 of 3', 'Validation iteration 2 of 3', etc."
      "5. If still failing after 3 attempts, report the remaining gaps to the developer and ask: force proceed, provide guidance, or abandon. Per D-11."

    - Post-plan section per D-09/D-13:
      "After plan is validated and approved, display:"
      "'Plan approved for Phase {N}: {task_count} tasks in {wave_count} waves.'"
      "'Ready to execute? (auto-continuing in 30 seconds...)'"
      "Wait for developer's response. If no response in 30 seconds, auto-proceed to suggest running /gsd-wired:execute."

    **Create internal/cli/plan.go** following NewResearchCmd pattern:
    - NewPlanCmd() returns *cobra.Command with Use: "plan", Short: "Create phase plan with dependencies"
    - RunE: prints "Planning must be run through /gsd-wired:plan slash command (requires Claude Code)" and exits with error.

    **Create internal/cli/plan_test.go** with:
    - TestRootCmdHasPlan: verify NewRootCmd().Commands() includes "plan" subcommand
    - TestPlanCmdOutput: verify running plan subcommand returns error with "slash command" in message

    **Update internal/cli/root.go**: add NewPlanCmd() to AddCommand chain.

    Run `go test ./internal/cli/... -race` -- all tests MUST pass.
    Run `go test ./... -race` -- full suite MUST pass.
  </action>
  <verify>
    <automated>go test ./internal/cli/... -run "TestRootCmdHasPlan|TestPlanCmdOutput" -race -v && test -f skills/plan/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - test -f skills/plan/SKILL.md
    - grep -q "create_plan_beads" skills/plan/SKILL.md
    - grep -q "query_by_label" skills/plan/SKILL.md
    - grep -q "flush_writes" skills/plan/SKILL.md
    - grep -q "Validation iteration" skills/plan/SKILL.md
    - grep -q "auto-continuing in 30 seconds" skills/plan/SKILL.md
    - grep -q "disable-model-invocation: true" skills/plan/SKILL.md
    - grep -q "Wave 1" skills/plan/SKILL.md
    - grep -q "func NewPlanCmd" internal/cli/plan.go
    - grep -q "TestRootCmdHasPlan" internal/cli/plan_test.go
    - grep -q "NewPlanCmd" internal/cli/root.go
    - go test ./internal/cli/... -race exits 0
    - go test ./... -race exits 0
  </acceptance_criteria>
  <done>/gsd-wired:plan SKILL.md exists with full plan generation, GSD-familiar display, and inline validation loop (up to 3 iterations per D-11). gsdw plan CLI subcommand registered and tested. CMD-03 satisfied. All tests pass.</done>
</task>

</tasks>

<verification>
- `go test ./... -race` passes (full regression)
- `test -f skills/plan/SKILL.md` (slash command exists)
- `grep -c "create_plan_beads" internal/mcp/tools.go` returns 1+ (tool registered)
- Tool count in server.go and tests is 13
- `grep -q "Validation iteration" skills/plan/SKILL.md` (plan checker loop exists)
- `grep -q "query_by_label" skills/plan/SKILL.md` (requirement coverage check exists)
</verification>

<success_criteria>
- /gsd-wired:plan slash command file exists and auto-discovers (PLAN-01, CMD-03)
- create_plan_beads creates task beads with topological dependency resolution (PLAN-02)
- Each task bead has acceptance criteria, complexity, and files in metadata (PLAN-03)
- Plan checker validates requirement coverage inline up to 3 iterations (PLAN-04, D-11, D-12)
- GSD-familiar wave/task display format (D-08)
- 30-second auto-proceed after plan approval (D-09, D-13)
- 13 MCP tools registered and tested
</success_criteria>

<output>
After completion, create `.planning/phases/06-research-planning/06-02-SUMMARY.md`
</output>
