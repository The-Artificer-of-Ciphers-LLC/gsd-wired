---
phase: 07-execution-verification
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - skills/execute/SKILL.md
  - skills/verify/SKILL.md
  - skills/ready/SKILL.md
autonomous: true
requirements: [EXEC-05, EXEC-06, CMD-07]
must_haves:
  truths:
    - "/gsd-wired:execute orchestrates wave-based parallel task execution through beads"
    - "Execution agents receive minimal 4-item context chain (task + acceptance + parent + deps)"
    - "Inline validation checks acceptance criteria between waves per D-07"
    - "/gsd-wired:verify presents pass/fail results and creates remediation tasks for failures"
    - "/gsd-wired:ready shows unblocked tasks via existing gsdw ready command"
  artifacts:
    - path: "skills/execute/SKILL.md"
      provides: "Execution orchestration slash command"
      contains: "name: execute"
    - path: "skills/verify/SKILL.md"
      provides: "Verification results slash command"
      contains: "name: verify"
    - path: "skills/ready/SKILL.md"
      provides: "Ready tasks display slash command"
      contains: "name: ready"
  key_links:
    - from: "skills/execute/SKILL.md"
      to: "internal/mcp/execute_wave.go"
      via: "MCP tool call execute_wave"
      pattern: "execute_wave"
    - from: "skills/execute/SKILL.md"
      to: "internal/mcp/tools.go"
      via: "MCP tool calls claim_bead and close_plan in agent prompts"
      pattern: "claim_bead"
    - from: "skills/verify/SKILL.md"
      to: "internal/mcp/verify_phase.go"
      via: "MCP tool call verify_phase"
      pattern: "verify_phase"
    - from: "skills/verify/SKILL.md"
      to: "internal/mcp/create_plan_beads.go"
      via: "MCP tool call create_plan_beads for remediation (D-10)"
      pattern: "create_plan_beads"
---

<objective>
Create three SKILL.md files for /gsd-wired:execute (wave execution orchestration), /gsd-wired:verify (phase verification), and /gsd-wired:ready (unblocked task display).

Purpose: These slash commands are the developer-facing interface per D-01. Execute orchestrates parallel Task() agents per wave (D-03). Verify presents pass/fail results and auto-creates remediation tasks (D-10/D-11). Ready wraps existing gsdw ready command (D-12).

Output: Three SKILL.md files enabling the complete execution and verification workflow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-execution-verification/07-RESEARCH.md
@.planning/phases/07-execution-verification/07-01-SUMMARY.md

<interfaces>
Existing SKILL.md patterns from skills/research/SKILL.md and skills/plan/SKILL.md:
- YAML frontmatter with name, description, disable-model-invocation, argument-hint
- Spawns parallel Task() agents with minimal prompts (bead ID + topic + 4 instructions)
- Each agent: claim_bead then work then close_plan
- 30-second auto-proceed after completion
- Inline validation loop: up to 3 iterations, explicit iteration tracking
- GSD-familiar display format (waves, tasks, dependencies)

execute_wave response shape: { wave: int, tasks: [{ bead_id, plan_id, title, acceptance_criteria, description, parent_summary, dep_summaries }] }

verify_phase response shape: { phase_num: int, passed: bool, results: [{ criterion, passed, method, detail }], failed: [string] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /gsd-wired:execute SKILL.md</name>
  <files>skills/execute/SKILL.md</files>
  <action>
Create skills/execute/SKILL.md following the research SKILL.md pattern for parallel agent spawning. Per D-03, the SKILL.md orchestrates wave execution.

SKILL.md frontmatter must have: name: execute, description: "Execute the current wave of unblocked tasks in parallel", disable-model-invocation: true, argument-hint: "[phase_number]"

Content structure (follow research/plan SKILL.md style exactly):

YOUR ROLE SECTION:
You are an execution coordinator. You run waves of parallel tasks through beads. Never mention beads, bead IDs, or graph structures to the developer. Use plan IDs (e.g., 07-01) in all developer-facing output.

WORKFLOW SECTION with numbered steps:

Step 1: Determine phase number from $ARGUMENTS or project context (call get_status if needed).

Step 2: Call execute_wave MCP tool with phase_num. Receive wave number and tasks array with context chains.

Step 3: If tasks array is empty, display "All tasks complete for Phase {N}." Auto-proceed to /gsd-wired:verify after 30 seconds per D-02.

Step 4: Display current wave in GSD table format showing plan_id, title, complexity. Include "Spawning {count} parallel agents..." message.

Step 5: Spawn parallel Task() agents -- one per task in the wave. Use this MINIMAL prompt template per D-04 (Pitfall 2 -- keep under 20 lines):

  You are an execution agent for task {plan_id}: {title}.
  Your bead ID is {bead_id}.
  Context:
  - Task: {description}
  - Acceptance: {acceptance_criteria}
  - Phase goal: {parent_summary}
  - Dependencies completed: {dep_summaries joined by newline}
  Instructions:
  1. Call claim_bead with id={bead_id}
  2. Implement the task described above
  3. Run: git add {relevant files} then git commit -m "feat({plan_id}): {title}"
  4. Call close_plan with id={bead_id} and reason={one-line summary of what was done}

CRITICAL per Pitfall 3: Agents only call claim_bead and close_plan. No query_by_label, get_bead, or other graph tools.
CRITICAL per D-06/EXEC-05: Commit message format is feat({plan_id}): {title} -- plan ID, not bead ID.

Step 6: Wait for all Task() agents to complete.

Step 7: Inline validation per D-07 -- for each completed task, verify acceptance criteria. Check file existence for files mentioned in acceptance. Run go test ./... if acceptance mentions tests. Best-effort only (Pitfall 6) -- do not do full code review.

Step 8: If validation fails per D-08, surface to developer with options: retry, skip, abort. Auto-continue with 'skip' after 30 seconds.

Step 9: Go to Step 2 (next wave). The execute_wave call returns newly unblocked tasks per EXEC-04.

POST-EXECUTION SECTION:
After all waves complete (Step 3 reached): display completion message with task and wave counts. Auto-proceed to /gsd-wired:verify {N} after 30 seconds per D-02.
  </action>
  <verify>
    <automated>test -f /Users/trekkie/projects/gsd-wired/skills/execute/SKILL.md && grep -q "name: execute" /Users/trekkie/projects/gsd-wired/skills/execute/SKILL.md && grep -q "execute_wave" /Users/trekkie/projects/gsd-wired/skills/execute/SKILL.md && grep -q "claim_bead" /Users/trekkie/projects/gsd-wired/skills/execute/SKILL.md && grep -q "close_plan" /Users/trekkie/projects/gsd-wired/skills/execute/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - skills/execute/SKILL.md exists with frontmatter name: execute
    - Workflow uses execute_wave MCP tool for context chains
    - Agent prompt template is minimal (4 instructions, under 20 lines)
    - Agents only call claim_bead and close_plan (no graph queries per Pitfall 3)
    - Commit message format uses plan_id not bead_id per D-06
    - Inline validation is best-effort per D-07/Pitfall 6
    - Developer escalation on failure per D-08
    - 30-second auto-proceed to /gsd-wired:verify per D-02
  </done>
</task>

<task type="auto">
  <name>Task 2: /gsd-wired:verify and /gsd-wired:ready SKILL.md files</name>
  <files>skills/verify/SKILL.md, skills/ready/SKILL.md</files>
  <action>
Create two SKILL.md files: verify (per D-09/D-10/D-11) and ready (per D-12/CMD-07).

SKILLS/VERIFY/SKILL.MD:

Frontmatter: name: verify, description: "Verify phase completion against success criteria", disable-model-invocation: true, argument-hint: "[phase_number]"

Your role: You are a verification coordinator. Check phase success criteria against the actual codebase. Present results in GSD-familiar format. Never mention beads or graph structures.

Workflow:

Step 1: Determine phase number from $ARGUMENTS or project context.

Step 2: Call verify_phase MCP tool with phase_num and project_dir=".".

Step 3: Display results as pass/fail table per D-11 with columns: #, Criterion, Status (PASS/FAIL), Method, Detail. Include "Result: {passed_count}/{total_count} criteria passed" summary.

Step 4: If all pass: "Phase {N} verified successfully. All {count} criteria pass." Then "Advancing to next phase... (auto-continuing in 30 seconds...)" per D-02.

Step 5: If failures exist (per D-10/VRFY-03): "Phase {N} verification found {fail_count} failing criteria." Then "Creating remediation tasks..."

Call create_plan_beads with remediation tasks -- one per failed criterion:
- id: "{phase}-fix-{N}" (e.g., "07-fix-1")
- title: "Fix: {failed criterion detail}" (use detail, not raw criterion per Pitfall 4)
- acceptance: the original criterion text
- context: "Remediation for failed verification criterion: {detail}"
- complexity: "S" (remediation tasks are typically small)
- files: [] (unknown until agent investigates)

Then call flush_writes.

Step 6: Display remediation plan showing plan_id and title. Auto-proceed to /gsd-wired:execute {N} after 30 seconds per D-02.

SKILLS/READY/SKILL.MD:

Frontmatter: name: ready, description: "Show unblocked tasks ready to work on", disable-model-invocation: false, argument-hint: "[phase_number]"

NOTE: disable-model-invocation: false -- ready is a lightweight informational command, safe for auto-invocation.

Your role: Display unblocked tasks. This is a thin wrapper over existing infrastructure.

Workflow:

Step 1: Determine if $ARGUMENTS contains a phase number.

Step 2: Call list_ready MCP tool (no arguments -- returns all ready beads).

Step 3: Display results in GSD wave format, grouped by phase. Table columns: Task (plan_id), Title, Reqs. If $ARGUMENTS has a phase number, filter to that phase only. Include total ready count.

Suggest gsdw ready or gsdw ready --phase {N} as CLI alternative.
  </action>
  <verify>
    <automated>test -f /Users/trekkie/projects/gsd-wired/skills/verify/SKILL.md && test -f /Users/trekkie/projects/gsd-wired/skills/ready/SKILL.md && grep -q "name: verify" /Users/trekkie/projects/gsd-wired/skills/verify/SKILL.md && grep -q "verify_phase" /Users/trekkie/projects/gsd-wired/skills/verify/SKILL.md && grep -q "create_plan_beads" /Users/trekkie/projects/gsd-wired/skills/verify/SKILL.md && grep -q "name: ready" /Users/trekkie/projects/gsd-wired/skills/ready/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - skills/verify/SKILL.md exists with pass/fail table display per D-11
    - Verify SKILL.md calls create_plan_beads for remediation per D-10/VRFY-03
    - Verify SKILL.md auto-proceeds after 30 seconds per D-02
    - skills/ready/SKILL.md exists as thin wrapper over list_ready per D-12
    - Ready SKILL.md displays in GSD wave format
    - No beads/graph terminology in developer-facing output
  </done>
</task>

</tasks>

<verification>
All three SKILL.md files exist with correct frontmatter and MCP tool references:
```bash
cd /Users/trekkie/projects/gsd-wired && \
  grep -l "name: execute" skills/execute/SKILL.md && \
  grep -l "name: verify" skills/verify/SKILL.md && \
  grep -l "name: ready" skills/ready/SKILL.md && \
  echo "All SKILL.md files verified"
```
</verification>

<success_criteria>
- /gsd-wired:execute orchestrates parallel wave execution with minimal agent prompts
- /gsd-wired:verify presents results and creates remediation tasks automatically
- /gsd-wired:ready shows unblocked tasks (CMD-07)
- All 3 SKILL.md files follow established patterns from research/plan
- 30-second auto-proceed pattern throughout per D-02
- No beads terminology in developer-facing output per D-01
</success_criteria>

<output>
After completion, create `.planning/phases/07-execution-verification/07-03-SUMMARY.md`
</output>
