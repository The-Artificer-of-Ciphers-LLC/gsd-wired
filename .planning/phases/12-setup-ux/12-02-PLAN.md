---
phase: 12-setup-ux
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - internal/cli/setup.go
  - internal/cli/setup_test.go
  - internal/cli/root.go
autonomous: true
requirements: [SETUP-01, SETUP-05]

must_haves:
  truths:
    - "gsdw setup runs check-deps first and shows results before offering installs"
    - "gsdw setup offers brew, go install, or binary download for each missing dep"
    - "gsdw setup does not silently install anything — always shows what will happen and waits for confirmation"
    - "gsdw setup is idempotent — re-running skips already-installed deps"
  artifacts:
    - path: "internal/cli/setup.go"
      provides: "gsdw setup interactive wizard"
      exports: ["NewSetupCmd"]
  key_links:
    - from: "internal/cli/setup.go"
      to: "internal/deps/check.go"
      via: "deps.CheckAll() to detect missing deps before offering install"
      pattern: "deps\\.CheckAll"
    - from: "internal/cli/root.go"
      to: "internal/cli/setup.go"
      via: "root.AddCommand(NewSetupCmd())"
      pattern: "NewSetupCmd"
---

<objective>
Create `gsdw setup` interactive wizard that guides developers through dependency installation.

Purpose: After `gsdw check-deps` tells a developer what is missing, `gsdw setup` walks them through fixing it with install method choices (brew, go install, binary download). This is the "one command to get started" entry point per SETUP-01.

Output: `gsdw setup` subcommand that runs check-deps, offers install methods for missing deps, and confirms environment is ready.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-setup-ux/12-01-SUMMARY.md

@internal/cli/root.go
@internal/deps/check.go
</context>

<interfaces>
<!-- From Plan 01 — deps package contracts -->

From internal/deps/check.go (created in Plan 01):
```go
type Status string
const (
    StatusOK   Status = "ok"
    StatusWarn Status = "warn"
    StatusFail Status = "fail"
)

type Dep struct {
    Name        string
    Binary      string
    Status      Status
    Version     string
    Path        string
    InstallHelp string
}

type CheckResult struct {
    Deps  []Dep
    AllOK bool
}

func CheckAll() CheckResult
```

From internal/cli/root.go:
```go
root.AddCommand(NewVersionCmd(), ..., NewCheckDepsCmd(), NewDoctorCmd())
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create gsdw setup interactive wizard</name>
  <files>internal/cli/setup.go, internal/cli/setup_test.go, internal/cli/root.go</files>
  <read_first>internal/deps/check.go (CheckAll API from Plan 01), internal/cli/checkdeps.go (renderCheckDeps output pattern), internal/cli/root.go (AddCommand chain)</read_first>
  <behavior>
    - Test: setup runs check-deps and displays results first (per D-06)
    - Test: when all deps present, setup prints "All dependencies satisfied" and exits cleanly
    - Test: when deps missing, setup prints install methods for each (per D-07, SETUP-05):
      - brew option (if brew is on PATH)
      - go install option (for bd, dolt)
      - binary download URL option
    - Test: setup prompts for user choice per missing dep (not auto-install, per anti-features)
    - Test: after user selects install method, setup prints the command to run
    - Test: setup re-checks deps after install attempt and reports updated status
    - Test: setup mentions container runtime and connection config are available via separate commands (per D-08 — deferred to Phase 13/14)
  </behavior>
  <action>
    Create `internal/cli/setup.go` with `NewSetupCmd() *cobra.Command`:

    1. **Phase 1 — Check current state:**
       - Call `deps.CheckAll()` to get current status
       - Render results using same format as check-deps (reuse renderCheckDeps or call it)
       - If AllOK, print "All dependencies satisfied. Environment ready." and return

    2. **Phase 2 — Offer install methods for each missing dep (per D-06, D-07, SETUP-05):**
       - For each dep with Status == "fail":
         - Print: "Missing: {dep.Name}"
         - Detect if `brew` is on PATH via exec.LookPath("brew")
         - Present numbered options:
           ```
           Install {dep.Name}:
             [1] brew install {formula}
             [2] go install {package}@latest
             [3] Download from {url}
             [s] Skip
           ```
         - Read user choice from stdin (bufio.NewReader, NOT bubbletea per anti-features)
         - Print the selected install command for user to run
         - Do NOT execute the install command automatically (per anti-features: "Print the install command, let the user run it")

    3. **Phase 3 — Re-verify after installs:**
       - After presenting all install options, prompt: "Press Enter after installing to re-check..."
       - Re-run deps.CheckAll() and display updated results
       - If still missing deps, print which ones and suggest re-running `gsdw setup`

    4. **Phase 4 — Next steps guidance (per D-08):**
       - Print:
         ```
         Next steps:
           - Container runtime: gsdw container setup (Phase 13)
           - Connection config: gsdw connect (Phase 14)
           - Health check: gsdw doctor
         ```
       - Container and connection commands don't exist yet — print them as future guidance

    **Testability:**
    - Extract `runSetup(in io.Reader, out io.Writer, checkFn func() deps.CheckResult, brewAvailable bool) error`
    - Tests inject fake io.Reader with predetermined responses and a mock checkFn
    - No actual binary installation in tests

    **root.go:**
    Add `NewSetupCmd()` to the root.AddCommand chain.

    Install method details per dep (per D-07, SETUP-05):
    - **bd:** `brew install steveyegge/tap/beads` | `go install github.com/steveyegge/beads/cmd/bd@latest`
    - **dolt:** `brew install dolthub/tap/dolt` | `curl -L https://github.com/dolthub/dolt/releases/latest/download/install.sh | bash`
    - **Go:** `brew install go` | download from https://go.dev/dl/
    - **Container runtime:** `brew install --cask docker` | `brew install podman`
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-wired && go test ./internal/cli/ -run "TestSetup" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "NewSetupCmd" internal/cli/setup.go
    - grep -q "NewSetupCmd" internal/cli/root.go
    - grep -q "deps.CheckAll" internal/cli/setup.go
    - grep -q "runSetup" internal/cli/setup.go
    - grep -q "io.Reader" internal/cli/setup.go
    - grep -q "func TestSetup" internal/cli/setup_test.go
    - go test ./internal/cli/ -run "TestSetup" passes
    - go build ./cmd/gsdw compiles clean
  </acceptance_criteria>
  <done>gsdw setup runs check-deps, offers install methods for each missing dep (brew/go install/download), reads user choice, re-verifies after install, and guides to next steps. All tests pass, binary compiles.</done>
</task>

</tasks>

<verification>
- `go test ./internal/deps/ ./internal/cli/ -count=1` passes (full test suite including Plan 01)
- `go build ./cmd/gsdw` compiles without errors
- `go vet ./...` reports no issues
</verification>

<success_criteria>
- gsdw setup runs check-deps first and shows current dependency status
- For each missing dep, setup offers brew, go install, or download options
- Setup never installs silently — shows command and waits for user
- Setup re-checks after install and reports updated status
- Setup guides to container/connection commands as next steps
- Command registered in root.go, binary compiles, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-setup-ux/12-02-SUMMARY.md`
</output>
